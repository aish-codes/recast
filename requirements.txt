# Runtime deps for the Vercel Python function.
# Kept in step with pyproject.toml [project.dependencies], minus the CLI-only bits.
pydantic>=2.9
openai>=1.55
python-dotenv>=1.0
jinja2>=3.1
reportlab>=4.2
pypdf>=5.1
python-docx>=1.1
fastapi>=0.115
python-multipart>=0.0.20
psycopg[binary]>=3.2
pyjwt[crypto]>=2.10          # verify Supabase access tokens locally
certifi>=2024.8              # CA bundle for the JWKS fetch
